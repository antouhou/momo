# Temporary Wayfire config used only to host momo-greeter.
# The real user session runs its own Wayfire config after login.
[core]
plugins = autostart

[autostart]
momo-greeter = /usr/local/bin/momo-greeter --session-command /usr/libexec/momo-shell-wayfire
autostart_wf_shell = false
